{
  "name": "fleeting-marks",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm --workspace apps/web run build && npm --workspace apps/api run build",
    "dev:web": "npm --workspace apps/web run dev",
    "dev:api": "npm --workspace apps/api run dev",
    "zip": "git ls-files | zip -@ fleeting-marks.zip"
  }
}
